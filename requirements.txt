# Python 3.11 / 3.12. pylance is the Lance SDK.
pylance==11.0.0
pyarrow==22.0.0
numpy>=1.26,<3
av==16.1.0
huggingface-hub==0.36.2
transformers==4.57.6
torch>=2.6,<3
Pillow>=10,<13
# CLAP preprocessing dependencies; the audio model is loaded only if needed.
scipy>=1.12,<2
librosa>=0.11,<0.12
